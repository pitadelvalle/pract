<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="widt=device-width, initial-scales=1.0"> 
    <link rel="stylesheet">
    < href ="//maxcd.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
</head>
<body>
    <div class="navbar navbar-defailt navbar-static-top" role="navigation">
    <div class="container">
    <div class="navbar-header">
    <a class="navbas-brand" href="/">Zombie Nest</a>
    </div>
    <ul class="nav navbar-nav navbar-right">
        <% if (currentZombie{%>
            <li>
                <a href="/edit">
                    Hola,<%= currentZombie.name()%>
                </a>
            </li>
            <li>
                <a href="/logoout">Salir</a>
            </li>
            <%} else {%>
                <li>
                    <a href="/login">Entrar</a>
                </li>
                <li>
                    <a href="/sigup">Registrarse</a>
                </li>
                <% } %>
            </ul>
                </div>
                </div>
                <div class="container">
                    <% errors.forEach(function(error){ %>
                        <div class="alert alert-danger" role="alert">
                            <% = error %>
                        </div>
                    <% })%>
                    <% infos forEach(function(info){%>
                        <div class="alert alert-info" role="alert">
                            <%=info%>
                        </div>
                    <%  }) %>
                </ul>)
</body>
</html>